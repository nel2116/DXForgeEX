<UserControl x:Class="DXForgeEditor.Editors.TextureView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:ed="clr-namespace:DXForgeEditor.Editors"
             d:DataContext="{d:DesignInstance Type=ed:TextureEditor}"
             d:DesignHeight="450" d:DesignWidth="800" FocusVisualStyle="{x:Null}"
             Focusable="True" mc:Ignorable="d">
    <Grid>
        <Grid ClipToBounds="True" Background="{StaticResource TiledGridBackground}"
              MouseRightButtonDown="OnGrid_Mouse_RBD" MouseRightButtonUp="OnGrid_Mouse_RBU"
              MouseWheel="OnGrid_Mouse_Wheel" PreviewMouseMove="OnGrid_Mouse_Move" x:Name="backgroundGrid">
            <Grid.LayoutTransform>
                <ScaleTransform ScaleX="{Binding ScaleFactor}" ScaleY="{Binding ScaleFactor}"/>
            </Grid.LayoutTransform>

            <Canvas IsHitTestVisible="False">
                <Rectangle Canvas.Left="{Binding PanOffset.X}" Canvas.Top="{Binding PanOffset.Y}"
                           Width="{Binding Path=ActualWidth, ElementName=textureImage}"
                           Height="{Binding Path=ActualHeight, ElementName=textureImage}">
                    <Rectangle.Fill>
                        <ImageBrush x:Name="textureBackground" ImageSource="/PrimalEditor;component/Resources/TextureEditor/Checker64.png"
                                    TileMode="Tile" Viewport="0,0,64,64" ViewportUnits="Absolute"/>
                    </Rectangle.Fill>
                </Rectangle>
            </Canvas>

            <Canvas IsHitTestVisible="False">
                <Image x:Name="textureImage" Canvas.Left="{Binding PanOffset.X}" Canvas.Top="{Binding PanOffset.Y}"
                       Source="{Binding SelectedSliceBitmap}"/>
            </Canvas>

        </Grid>
    </Grid>
</UserControl>
